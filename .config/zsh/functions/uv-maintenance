echo "Cleaning UV cache..."
uv cache clean
echo "Updating UV tools..."
for tool in $(uv tool list | grep -v "^$" | cut -d' ' -f1)
do
	uv tool install --force "$tool"
done
echo "Updating mise shims..."
mise reshim
echo "Verifying cache permissions..."
chmod 777 ~/.cache/uv
