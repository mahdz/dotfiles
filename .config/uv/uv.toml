# UV configuration optimized for mise integration
python-preference = "only-managed"
python-downloads = "manual"

# Performance optimizations
concurrent-downloads = 4
concurrent-builds = 4

# Cache settings
# cache-dir = "~/.cache/uv"

# Security settings
keyring-provider = "subprocess"

# Ensure UV tools are always compatible with the managed Python
compile-bytecode = true
